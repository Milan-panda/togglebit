[project]
name = "togglebit-api"
version = "0.1.0"
description = "Togglebit feature flag evaluation API"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
